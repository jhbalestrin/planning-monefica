# Copy to project root as `.env` and run: docker compose --env-file .env up --build
# (Compose reads this file for variable substitution in docker-compose.yml.)

JWT_SECRET=docker-dev-change-me

# Optional: bake into ic-app image (rebuild ic-app after changing)
# EXPO_PUBLIC_TENANT_ID=
# EXPO_PUBLIC_DEV_ACCESS_TOKEN=
