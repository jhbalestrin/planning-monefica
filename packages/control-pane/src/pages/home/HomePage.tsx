/**
 * Route entry for the home page module. Implementation is split across
 * containers/, components/, and api/ (see docs/page-module-patterns.md).
 */
export { HomeContainer as HomePage } from './containers/HomeContainer';
